var students = ["Сергей", "Матвей", "Лейла", "Алина", "Иоган"];
    if (students.length >=3) {
 alert ("Это большой массив");
} else {
 alert ("Это маленький массив");
}

(students.length >=3) ? alert ("Это большой массив") : alert ("Это маленький массив")